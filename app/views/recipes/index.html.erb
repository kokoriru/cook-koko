<h1>Recipe index</h1>

<table>
  <tr>
    <th>レシピ名</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>
  <% @recipes.each do |recipe| %>
  <tr>
    <td><%= recipe.name %></td>
    <td><%= link_to 'Show', recipe %></td>
    <td><%= link_to 'Edit', edit_recipe_path(recipe) %></td>
    <td><%= link_to 'Destroy', recipe, data: { :confirm => 'Are you sure?' }, :method => :delete %></td>
  </tr>
  <% end %>
</table>
<%= link_to 'New', new_recipe_path %>
